RN static generation is not possible. To be exact SSG using nuxt generate is possible, but the system wont work for multiple reasons.
One reason is config loading. Normally the site tries to load config from the server during SSR and on the client with an API, (ORPC to be exact) but this OFC is not possible with SSG, there is no server. While statically generating the config needs to be loaded and then never changed. This is fine ofc, the static site would be a demo page so static config is fine. The config loading is done in layers/01-base/app/plugins/02-config.ts
Now idk how to make this happen but it should be something like that...
Other thing is actually loading data based on the config. Every component makes a request to the server, the server loads stuff. now this need to happen while generating ofc and the components rn i think are async components and data loading is not awaited therefore data is not baked into the generation.. i want to keep this behaviour for normal sites but for generation we need some check to await those datas and store them statically. For this there needs to be some tanstack query setting also because the useQuery would always refetch on the client even if there is no server. now this behaviour i also want to keep for normally but disable refetch when generated site.
the server also has a file watcher for configs which is useless for SSG but i think it works fine while SSG even when enabled so keep that untouched for now.
Also in the client side config plugin the actual SSE needs to be disabled ofc. and the UI also has an icon which represents the config loading status which ofc doesnt need to be there when statically generated.
figure this out